const router = require('express').Router();

const { messageController } = require('../controllers/message.controller');
//
// router.get('/:roomName',
//     messageController.getMessages);
//
// router.post('/:roomName',
//     messageController.postMessages);

module.exports = router;